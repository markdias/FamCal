# Podfile for FamCal
# Generated: November 24, 2025

platform :ios, '14.0'

project 'FamCal.xcodeproj'

target 'FamCal' do
  # Google Sign-In SDK for authentication
  # Note: Add this back and build in Xcode GUI to resolve module map conflicts
  # pod 'GoogleSignIn'

  # Google Mobile Ads SDK for AdMob integration
  # Temporarily removed due to build configuration issues
  # pod 'Google-Mobile-Ads-SDK'
end
